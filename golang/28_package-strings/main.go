package main

import (
	"fmt"
	"strings"
)

// strings Contains, Count, HasPrefix, HasSuffix, Index, Join, Repeat, Replace, Split, ToLower, ToUpper, TrimSpace

func main() {
	fmt.Println(strings.Replace("salamsalam", "a", "hi", 2))
	fmt.Println(string([]rune{'g', 'o', 'o', 'd'}))
}
